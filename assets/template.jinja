<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8">
  <title>Relatório Mensal</title>
  <style> {{ stylesheet }}</style>
</head>

<body>
  <img src="{{ logo_url }}" alt="Logotipo" style="width: 150px; height: auto;">
  <h1 class="title">{{ tipo_relatorio }}</h1>
  
  <p class="subtitle">Relatório gerado em {{ dia }} às {{ hora }}</p>

  <h2>{{ valores_cliente }}</h2>
  {{ parcelas_vencidas }}

  <h2>{{ valores_parcelas }}</h2>
  {{ devedores }}
  {{ bobagem }}
  
</body>

</html>

<div class="linha-horizontal"></div>

